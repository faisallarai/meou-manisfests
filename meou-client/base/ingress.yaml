apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend
  namespace: meuo
spec:
  rules:
    - http:
        paths:
          - path: /*
            pathType: Prefix
            backend:
              service:
                name: meou-react-service
                port:
                  number: 80
