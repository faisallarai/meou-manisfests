apiVersion: v1
kind: Service
metadata:
  name: meou-api-service
  namespace: meou
  labels:
    app: meou-api
    tier: api
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 3000
  selector:
    app: meou-api
    tier: api
