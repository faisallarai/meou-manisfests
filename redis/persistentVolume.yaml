apiVersion: v1
kind: PersistentVolume
metadata:
  name: efs-pv-redis
  namespace: meuo
spec:
  capacity:
    storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: efs-sc
  csi:
    driver: efs.csi.aws.com
    volumeHandle: fs-054620634643a3ff4
